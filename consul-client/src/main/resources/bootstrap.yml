spring:
  application:
    name: consul-client
  cloud:
    consul:
      host: 192.168.56.101 #ip
      port: 8500 #consul 服务器端口
      enabled: true
      discovery:
        hostname: 192.168.56.101 #consul 服务器主机名
        enabled: true #是否启用服务发现