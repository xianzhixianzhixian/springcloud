server:
  port: 8083

spring:
  application:
    name: eureka-consumer

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8081/eureka
    register-with-eureka: true
    fetch-registry: true